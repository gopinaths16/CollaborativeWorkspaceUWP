<UserControl
    x:Class="CollaborativeWorkspaceUWP.CustomControls.UserControls.IconedLabelControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CollaborativeWorkspaceUWP.CustomControls.UserControls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <StackPanel Orientation="Horizontal">
        <TextBlock x:Name="LabelIcon"
                   FontFamily="Segoe MDL2 Assets"
                   Text="{x:Bind Icon}"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   Margin="5, 0, 5, 0"
                   Foreground="Gray"/>
        <TextBlock x:Name="LabelContent"
                   Text="{x:Bind ContentText}"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   Foreground="Gray"/>
    </StackPanel>
</UserControl>
