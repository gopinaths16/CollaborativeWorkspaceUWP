<Page
    x:Class="CollaborativeWorkspaceUWP.Views.LoginView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CollaborativeWorkspaceUWP.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource MainBackgroundAcrylicBrush}">

    <Grid Background="{StaticResource MainBackgroundAcrylicBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBox Name="Username" PlaceholderText="Username" Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2"/>
        <TextBox Name="Password" PlaceholderText="Password" Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2"/>
        <Button Name="LoginButton" Content="Login" Grid.Row="2" Grid.Column="0"/>
        <Button Name="SingupButton" Content="Login" Grid.Row="2" Grid.Column="1"/>
    </Grid>
</Page>
